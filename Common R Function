## reading in xls packages
install.packages("xlsx")
library(xlsx)
mydata<- read.xlsx("./desktop/data/.xlsx")

Cleaning data
## Extracting appointment column without 1
head(mydata[mydata$appointment > 1])

## removing duplicated name
install.packages("dplyr")
library("dplyr")
distinct(mydata, PatName )

# Example remove duplicated rows based on Sepal.Length- iris data set
distinct(my_data, Sepal.Length)
distinct_(my_data,  "Sepal.Length", "Petal.Width")

